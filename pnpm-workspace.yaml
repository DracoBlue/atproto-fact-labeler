# pnpm 10+ moved build-script approval out of package.json. Listed packages
# are allowed to run their install scripts (native build for better-sqlite3,
# esbuild's prebuilt-binary download).
onlyBuiltDependencies:
  - better-sqlite3
  - esbuild

allowBuilds:
  better-sqlite3: true
  esbuild: true
