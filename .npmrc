# pnpm 10+: allow only these packages to run install scripts.
# better-sqlite3 needs a native build; esbuild needs to download its binary.
auto-install-peers=true
only-built-dependencies[]=better-sqlite3
only-built-dependencies[]=esbuild
